<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
	   http://code.alibabatech.com/schema/dubbo
	   http://code.alibabatech.com/schema/dubbo/dubbo.xsd
	   http://www.springframework.org/schema/tx
       http://www.springframework.org/schema/tx/spring-tx.xsd
       http://www.springframework.org/schema/aop
       http://www.springframework.org/schema/aop/spring-aop.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder location="classpath:config.properties"/>

    <dubbo:provider accesslog="true" retries="3" timeout="6000"/>

    <dubbo:application name="dubbo-analysis-provider" owner="gochinatv-cdn" organization="gochinatv"/>

    <dubbo:registry address="${dubbo.registry.address}"/>

    <dubbo:annotation package="com.gochinatv.cdn"/>

    <dubbo:protocol name="dubbo" port="${dubbo.protocol.dubbo.port}" serialization="kryo"/>

    <dubbo:protocol name="rest" port="${dubbo.protocol.rest.port}" threads="500" contextpath="analysis/services" server="tomcat" accepts="500"/>
</beans>